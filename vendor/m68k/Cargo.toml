[package]
name = "m68k"
version = "0.1.5"
edition = "2024"
license = "MIT"
description = "A safe Rust M68000 family CPU emulator"
exclude = ["tests/**/*"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
